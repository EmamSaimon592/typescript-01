✅ generic function with Array

function getFirstElement<T>(arr: T[]): T {
  return arr[0];
}

const firstNum = getFirstElement<number>([10, 20, 30]); // 10
const firstStr = getFirstElement(['a', 'b', 'c']); // "a"

console.log(firstNum);
console.log(firstStr);

....................................

✅ Aysnc function in ts
async function getData(): Promise<string> {
  throw new Error("Something went wrong!");
}

🔸 getData() হল একটা async function।
🔸 কিন্তু এটা কোন data return না করে, বরং error ছুঁড়ে দেয় (throw)।

async function run() {
  try {
    const data = await getData();  // এখানে error হবে
    console.log(data);
  } catch (error) {
    console.error("Error:", (error as Error).message); // error message দেখাবে
  }
}

🔸 run() ফাংশন getData()-কে কল করে await দিয়ে।
🔸 যেহেতু getData() error দেয়, তাই try block এ না গিয়ে catch block-এ চলে যায়।
🔸 catch block এ সেই error message দেখানো হয়।

 যদি Error না দিতাম?
async function getData(): Promise<string> {
  return "Hello world!";
}
তাহলে output হত:
Hello world!
------------------------------------------------------------


✅ Enum
1. Auto-incremented Values (সংখ্যা স্বয়ংক্রিয়ভাবে বাড়ে)
enum Color {
  Red,    // 0
  Green,  // 1
  Blue    // 2
}

2.Custom Numeric Values (নিজের মতো করে সংখ্যা দেওয়া):
enum Color {
  Red = 1,
  Green = 5,
  Blue = 10
}

3. String Enums (স্ট্রিং ভ্যালু সহ enum):
enum Direction {
  North = "NORTH",
  South = "SOUTH",
  East = "EAST",
  West = "WEST"
}

4. Heterogeneous Enum (number + string একসাথে)
enum Example {
  No = 0,
  Yes = "YES"
}

কেন enum ব্যবহার করবেন?
✅ কোড ক্লিন ও বোঝার সহজ
✅ ম্যাজিক নাম্বার এড়িয়ে যায়
✅ টাইপ সেফ – ভুল ভ্যালু ধরতে পারে কম্পাইল টাইমেই

✔শেষ কথা:
TypeScript এ enum একটি powerful feature, যা আপনার constant values গুলোকে সুন্দরভাবে handle করতে সাহায্য করে। এটা ব্যবহার করলে কোড বেশি readable ও maintainable হয়।

---------------------------

In TypeScript, OOPs (Object-Oriented Programming System) is fully supported using features like:

1.Classes and Objects 

2. Inheritance (উত্তরাধিকার) ---> use keyword( ✅extends)

3. Encapsulation (গোপনতা রক্ষা)---> public , private , protected /Encapsulation হলো OOP-এর principle, যা দিয়ে আমরা class-এর internal data বা properties কে hide করি।অন্য class বা object সরাসরি access করতে পারে না।শুধু methods এর মাধ্যমে access বা modify করা যায়।এই প্রক্রিয়াকে বলা হয় data hiding বা গোপনতা রক্ষা।

🔴 private = শুধুমাত্র class-এর ভিতর থেকে access করা যাবে.
🔴 protected = class এবং subclass থেকে access করা যাবে
🔴 public =  class, subclass, object সবখান থেকে access করা যাবে (default)

4. Polymorphism (বহুরুপতা)

5.Abstraction

6. Interfaces : 
Interface হচ্ছে TypeScript-এর এমন একটি feature, যা দিয়ে আমরা একটি object বা class-এর structure নির্ধারণ করতে পারি।

এটা একধরনের contract বা blueprint — যেখানে বলে দেওয়া হয়, একটি object বা class-এর মধ্যে কোন কোন property/মেথড থাকতে হবে, এবং সেগুলোর type কী হবে।

✅ Interface কাদের জন্য দরকার?

যখন আপনি চান সব object বা class একটি নির্দিষ্ট নিয়ম মেনে তৈরি হোক।

যখন আপনি OOP (Object-Oriented Programming) এ কাজ করেন।

বা Type Checking এ সাহায্য পেতে চান।
